package pong;

import java.awt.Color;

public class Menu {
    // menu screen

    Boundary boundary;
    
    public Menu (Boundary boundary)
    {
        this.boundary = boundary;
    }

    Button playButton = new Button(boundary.getxCenter(), boundary.getyCenter(), 0, 0)
}
